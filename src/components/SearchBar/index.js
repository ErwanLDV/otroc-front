import './style.scss';

function SearchBar() {
  return (
    <form>
      <input placeholder="rechercher un objet" />
      <button type="button"> rechercher </button>
    </form>
  );
}

export default SearchBar;
